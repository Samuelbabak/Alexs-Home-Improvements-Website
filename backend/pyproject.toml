[project]
name = "alexshomeimprovements"
version = "0.1.0"
description = "configure sending emails from the browser to the business email with security checks"
authors = [
    {name = "Your Name",email = "you@example.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard] (>=0.141.1,<0.142.0)",
    "dotenv (>=0.9.9,<0.10.0)",
    "pydantic (>=2.13.4,<3.0.0)",
    "requests (>=2.32.0,<3.0.0)",
    "redis (>=8.1.0,<9.0.0)",
]

[tool.poetry]
package-mode = false


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
